org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Mapbox Downloads Token for Maven repository access
# This token is loaded from local.properties (not committed to git)
# See local.properties.example for setup instructions
MAPBOX_DOWNLOADS_TOKEN=YOUR_TOKEN_HERE
